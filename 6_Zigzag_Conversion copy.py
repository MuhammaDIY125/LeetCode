# Zigzag Conversion
class Solution:
    def convert(self, s: str, numRows: int) -> str:
        lst = [[]] * numRows
        n = numRows

s1 = Solution()
s = "ABCD" # 1 <= s.length <= 1000
numRows = 4 # 1 <= numRows <= 1000
print(s1.convert(s, numRows))